# Push Chain RPC (Donut testnet)
VITE_PUSH_RPC=https://rpc.pushchain.example/donut
# Relayer server
VITE_RELAYER_URL=http://localhost:8787
# Private key for relayer (TESTNET ONLY!)
RELAYER_PRIVATE_KEY=0xabc...
# Auth secret for JWT signing
AUTH_SECRET=your-secret-key
